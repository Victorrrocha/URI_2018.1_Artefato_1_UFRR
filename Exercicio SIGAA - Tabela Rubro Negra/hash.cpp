#include <iostream>
#include <stdio.h>
#include <stdlib.h>
#include "arvRB.h"

using namespace std;

void inserir_hash(Arv* tab_hash, int mod, int numero)
{
   // inserir(tab_hash[10], numero);
}

int mod(int n, int m)
{
    return n%m;
}
